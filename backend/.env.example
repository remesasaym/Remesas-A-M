# 🔒 Variables de Entorno - Backend

# Supabase
SUPABASE_URL=tu_supabase_url_aqui
SUPABASE_ANON_KEY=tu_supabase_anon_key_aqui
SUPABASE_SERVICE_ROLE_KEY=tu_service_role_key_aqui

# Gemini AI
GEMINI_API_KEY=tu_gemini_api_key_aqui

# Encriptación (generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ENCRYPTION_KEY=tu_encryption_key_de_64_caracteres_hex

# Admin
ADMIN_EMAIL=tu_email_admin@ejemplo.com
ADMIN_UID=tu_admin_uid_de_supabase

# CORS (separar múltiples orígenes con comas)
ALLOWED_ORIGINS=http://localhost:5173,https://tu-dominio.com

# Webhook Secrets (generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
THUNES_WEBHOOK_SECRET=tu_thunes_webhook_secret
PERSONA_WEBHOOK_SECRET=tu_persona_webhook_secret

# Thunes API (opcional - para producción)
THUNES_API_KEY=
THUNES_API_SECRET=
THUNES_BASE_URL=https://api-sandbox.thunes.com
THUNES_ENV=sandbox

# Persona KYC (opcional - para producción)
PERSONA_API_KEY=
PERSONA_TEMPLATE_ID=
PERSONA_ENV=sandbox

# IMPORTANTE: 
# 1. NO subir este archivo a Git
# 2. Copiar a .env para desarrollo local
# 3. En producción, usar Secrets Manager (Vercel/Railway/etc)
